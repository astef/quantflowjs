export function OpenSession(instrumentId) {
  this.instrumentId = instrumentId;
}

OpenSession.prototype = {};

export function CloseSession(instrumentId) {
  this.instrumentId = instrumentId;
}

OpenSession.prototype = {};
