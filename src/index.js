import lny from "./model/lnyield.js";
import _ from "lodash";

console.log(_.get({ x: { y: { z: 42 } } }, "x.y.z"));

if (!__DEV__) {
  foo();
}
if (a.__DEV__) {
  foo();
}

while (true) {
  console.log("asdasd" === "asd" + "asd");
  // process inputs
  // process program
  // process outputs

  if (a.__DEV__) {
    console.log("asdasd" === "asd" + "asd");
  }
}
